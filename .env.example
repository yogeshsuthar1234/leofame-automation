# Which free trial to claim: followers | likes | views
CLAIM_TYPE=likes

# Used when CLAIM_TYPE=followers (your own Instagram username, public profile)
INSTAGRAM_USERNAME=your_username_here

# Used when CLAIM_TYPE=likes or CLAIM_TYPE=views (link to your own public post).
# Optional: leave blank to auto-pick the next post from data/posts.json (round-robin) instead.
INSTAGRAM_POST_URL=

# Set to false to watch the browser locally, true for headless (CI default)
HEADLESS=true

# Instagram account whose posts are tracked in data/posts.json (used by the fetch/sync scripts)
TARGET_IG_ACCOUNT=dadaji_furniture_vadodara
